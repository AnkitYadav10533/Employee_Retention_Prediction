# -*- coding: utf-8 -*-
"""employee_retention_logistic_regression.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1Y3kCTgC8X0-YuHlzX07iOfxf0HqgtTfD
"""

import pandas as pd
import numpy as np



df=pd.read_csv('/content/sample_data/HR_comma_sep.csv')

df.head(10)

"""# cleaning"""



df.shape

"""check null values

"""

df.isnull().sum()

"""check columns"""

df.columns

df.info()

df.describe()

df.describe(include='all')



"""#check duplicated values"""

df.duplicated()

df['satisfaction_level'].unique()

df['satisfaction_level'].value_counts()
#

df['left'].value_counts()

df.duplicated().sum()

"""#remove duplicates"""

df.drop_duplicates()

df.duplicated().sum()

df.drop_duplicates(inplace=True)

df.duplicated().sum()

df.shape

"""#Exploratory Data Analysis (EDA)"""

df['left'].value_counts()
# EDA hepls to identify the reason of retention

#find average or summaries
df.groupby('left').mean(numeric_only=True)
#it shows that who has satisfaction level higher then .66 willl stay below .44 leave

df.groupby('left').mean(numeric_only=True)

"""retention question 2: Salary vs Employee Retention"""

pd.crosstab(df['salary'],df['left'])

"""plot the bar chart

"""

import matplotlib.pyplot as plt
pd.crosstab(df['salary'], df['left']).plot(kind='bar')
plt.title('Salary vs Employee Retention')
plt.xlabel('salary')
plt.ylabel('left')
plt.show()

"""Depratment vs Employee retention

"""

pd.crosstab(df['Department'], df['left'])

pd.crosstab(df['Department'], df['left']).plot(kind='bar')
plt.title('Department vs Employee Retebtion')
plt.xlabel('Department')
plt.ylabel('left')
plt.show()

"""#Logistic regression Model"""

# Select Features (X)
#         ↓
# Select Target (y)
#         ↓
# Convert Categorical Data
#         ↓
# Train-Test Split
#         ↓
# Import Logistic Regression
#         ↓
# Train Model
#         ↓
# Predict
        # ↓
# Measure Accuracy

X=df[['satisfaction_level',
      'average_montly_hours','promotion_last_5years','salary']]
y=df['left']

df.info()

salary_dummies=pd.get_dummies(df['salary'],prefix='salary')

salary_dummies.head()

X=X.drop('salary', axis='columns') #x axis
X.head()
# y=df['left']

"""#train-Test Split

"""

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test=train_test_split(X,y,test_size=0.2,random_state=10)
from sklearn.linear_model import LogisticRegression
model=LogisticRegression()
model.fit(X_train, y_train)
y_pred=model.predict(X_test)
from sklearn.metrics import accuracy_score
accuracy=accuracy_score(y_test, y_pred)
print('Accuracy:',accuracy)

X.dtypes

"""#Prediction"""

model.score(X_train,y_train)

from sklearn.metrics import confusion_matrix

cm = confusion_matrix(y_test, y_pred)
print(cm)

"""#Accuracy"""

from sklearn.metrics import classification_report

print(classification_report(y_test, y_pred))

